App de cálculo de IMC, desenvolvido em dart com testes unitários.

comando para baixar dependencias
 - dart pub get

comando para gerar mock:
- dart run build_runner build --delete-conflicting-outputs
